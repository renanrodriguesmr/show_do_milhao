import { AnswerResultPipe } from './answer-result.pipe';

describe('AnswerResultPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerResultPipe();
    expect(pipe).toBeTruthy();
  });
});
