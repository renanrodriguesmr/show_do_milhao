import { FinalResultPipe } from './final-result.pipe';

describe('FinalResultPipe', () => {
  it('create an instance', () => {
    const pipe = new FinalResultPipe();
    expect(pipe).toBeTruthy();
  });
});
